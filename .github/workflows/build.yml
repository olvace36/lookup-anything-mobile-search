name: Build LookupAnythingMobileSearch

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Build
        run: dotnet build LookupAnythingMobileSearch.csproj --configuration Release

      - name: Package mod
        run: |
          mkdir -p output/LookupAnythingMobileSearch
          cp bin/Release/net6.0/LookupAnythingMobileSearch.dll output/LookupAnythingMobileSearch/
          cp manifest.json output/LookupAnythingMobileSearch/
          cd output && zip -r LookupAnythingMobileSearch.zip LookupAnythingMobileSearch/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: LookupAnythingMobileSearch
          path: output/LookupAnythingMobileSearch.zip
